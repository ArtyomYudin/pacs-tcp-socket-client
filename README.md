# Клиент TCP-сокета для СКУД Revers 8000

> 🚪 Сервис на Python для подключения к системе контроля и управления доступом **Revers 8000** с передачей событий в RabbitMQ и сохранением в PostgreSQL.

Сервис устанавливает постоянное TCP-соединение с контроллером СКУД Revers 8000, принимает события (проходы, тревоги, ошибки), **сохраняет их в базу данных PostgreSQL** и одновременно **публикует в шину сообщений RabbitMQ** для интеграции с IT-порталом поддержки.

---

## 📌 Основные функции

- Подключение к СКУД Revers 8000 по TCP-сокету
- Приём событий в реальном времени
- Сохранение всех событий в **PostgreSQL**
- Публикация данных в **RabbitMQ** (виртуальный хост `it_support`)
- Автоматическое восстановление соединения при обрыве
- Поддержка запуска в Docker-контейнере

---

## 🛠️ Требования

- Python ≥ 3.9
- Доступ к контроллеру Revers 8000 по сети
- RabbitMQ ≥ 3.8
- PostgreSQL ≥ 12
- Библиотеки: `pika`, `psycopg2`, `python-dotenv`, `logging`, `socket`

## Архитектура
```
Revers 8000 → TCP → [Этот сервис] → 
    ├─→ PostgreSQL (архив событий)
    └─→ RabbitMQ → Backend → Angular-фронтенд (IT Support Portal)
```